__pycache__/
*.py[cod]
*.pyo
*.pyd
*.db
*.sqlite3
*.egg-info/
.DS_Store
dist/
build/
.vscode/
.idea/
core/tests/fixtures/meta

# Built frontend copied into the API's static mount. Regenerable from
# frontend/ via the Dockerfile or scripts/build-frontend.sh. Not tracked
# (the bundle's hashed filenames change every build and would churn history).
api/src/ic_api/static/

# Derived training artifacts and visualizations — regenerable from
# core/data/train/ via core/scripts/convert_hufnagel_csv.py and
# core/scripts/run_pipeline.py. Not tracked to keep diffs clean.
core/data/derived/
